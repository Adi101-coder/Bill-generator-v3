{
  "name": "bill-generator-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "echo 'No build step required for Node.js server'",
    "postinstall": "node -e \"try { require('puppeteer'); } catch(e) { console.log('Puppeteer not found, installing...'); }\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "moment": "^2.29.4",
    "mongoose": "^7.5.0",
    "multer": "^1.4.5-lts.1",
    "puppeteer": "^21.11.0",
    "uuid": "^9.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "echo 'No build step required for Node.js server'",
    "postinstall": "node -e \"try { require('puppeteer'); } catch(e) { console.log('Puppeteer not found, installing...'); }\""
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
